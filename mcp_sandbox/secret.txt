This file exists only to prove the firewall's own arg-schema validation
(not just the MCP server's directory sandboxing) is what's actually being
tested — read it directly to confirm read_file works on any file inside
mcp_sandbox/, then try reading a path outside mcp_sandbox/ to see policy
deny it before the MCP server is ever called.
